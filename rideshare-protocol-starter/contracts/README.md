# Contracts

- `RideRegistry.sol` — Emits events for ride offers and accepts. Keeps state minimal.
- `RideEscrow.sol` — Holds funds, manages lifecycle, supports disputes/timeouts.

## Commands
```bash
forge build
forge test -vv
```
